Three different ways to extend Apertus tokenizer are provided here.

First use the vocab-extension/vocabularyGen to generate a list of candidate tokens from a corpus.

Then use one of the available three distillation scripts to create a new tokenizer that extends the base Apertus tokenizer with the new tokens.
